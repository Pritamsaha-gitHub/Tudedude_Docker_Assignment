version: "3"

services:
  frontend:
    build: ./frontend
    container_name: frontend_app
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: backend_app
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://spritam675:1234@cluster0.qo1o931.mongodb.net/?retryWrites=true&w=majority
